<UserControl x:Class="MyWPFCRUDApp.Views.TaxCategoryViews"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="GhostWhite">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Available Tax Categories" Margin="5">
            <DataGrid ItemsSource="{Binding TaxCategory}" 
                      SelectedItem="{Binding SelectedTaxCategory}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tax Name" Binding="{Binding CategoryName}" Width="*"/>
                    <DataGridTextColumn Header="Percentage (%)" Binding="{Binding TaxPercentage}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <StackPanel Grid.Column="1" Margin="10">
            <GroupBox Header="Tax Configuration" Padding="10">
                <StackPanel>
                    <Label Content="Tax Category Name (e.g. GST 18%)"/>
                    <TextBox Text="{Binding MTaxCategory.CategoryName, UpdateSourceTrigger=PropertyChanged}" Height="28" Margin="0,0,0,15"/>
                    
                    <Label Content="Tax Percentage (%)"/>
                    <TextBox Text="{Binding MTaxCategory.TaxPercentage, UpdateSourceTrigger=PropertyChanged}" Height="28" Margin="0,0,0,20"/>

                    <Button Content="Save Tax Category" Command="{Binding TaxSaveCommand}" Height="35" Margin="0,5" Background="#FF007ACC" Foreground="White" FontWeight="Bold"/>
                    <Button Content="Reset" Command="{Binding TaxResetCommand}" Height="30" Margin="0,5"/>
                    <Button Content="Delete Selected" Command="{Binding TaxDeleteCommand}" Height="30" Margin="0,5" Background="#FFFFEBEB" Foreground="DarkRed"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>
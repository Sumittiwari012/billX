<UserControl x:Class="MyWPFCRUDApp.Views.BankAccountViews"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MyWPFCRUDApp.Views"
             mc:Ignorable="d"
             Background="AliceBlue"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Registered Account Numbers" Margin="5">
            <DataGrid ItemsSource="{Binding BankAccounts}"
                      SelectedItem="{Binding SelectedAccount}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"             Binding="{Binding Id}"            Width="40"/>
                    <DataGridTextColumn Header="Account Number" Binding="{Binding AccountNumber}"  Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="5">
            <StackPanel>
                <GroupBox Header="Account Details" Margin="0,0,0,10">
                    <UniformGrid Columns="2" Margin="5">
                        <StackPanel Margin="5">
                            <Label Content="Account Number *"/>
                            <TextBox Text="{Binding MBankAccount.AccountNumber, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </UniformGrid>
                </GroupBox>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,5,20">
                    <Button Content="Reset"  Command="{Binding BankResetCommand}"  Width="90"  Height="30" Margin="5"/>
                    <Button Content="Delete" Command="{Binding BankDeleteCommand}" Width="90"  Height="30" Margin="5" Background="#FFFFEBEB" Foreground="DarkRed"/>
                    <Button Content="Save"   Command="{Binding BankSaveCommand}"   Width="130" Height="30" Margin="5" Background="#FF007ACC" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
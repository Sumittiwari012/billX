<UserControl x:Class="MyWPFCRUDApp.Views.CustomerViews"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="GhostWhite">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Customer List" Margin="5">
            <DataGrid ItemsSource="{Binding CustomerList}" 
                      SelectedItem="{Binding SelectedCustomer}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      AlternatingRowBackground="#F0F0F0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Customer Name" Binding="{Binding CustomerName}" Width="*"/>
                    <DataGridTextColumn Header="Mobile" Binding="{Binding MobileNumber}" Width="120"/>
                    <DataGridTextColumn Header="City" Binding="{Binding City}" Width="100"/>
                    <DataGridTextColumn Header="Balance" Binding="{Binding CurrentBalance, StringFormat={}{0:N2}}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="5">
            <StackPanel>
                <GroupBox Header="Personal Details" Margin="0,0,0,10">
                    <UniformGrid Columns="2" Margin="5">
                        <StackPanel Margin="5">
                            <Label Content="Customer Name*"/>
                            <TextBox Text="{Binding MCustomer.CustomerName, UpdateSourceTrigger=PropertyChanged}" Height="28"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <Label Content="Mobile Number*"/>
                            <TextBox Text="{Binding MCustomer.MobileNumber, UpdateSourceTrigger=PropertyChanged}" Height="28"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <Label Content="Email"/>
                            <TextBox Text="{Binding MCustomer.Email}" Height="28"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <Label Content="GSTIN"/>
                            <TextBox Text="{Binding MCustomer.GSTIN}" Height="28"/>
                        </StackPanel>
                    </UniformGrid>
                </GroupBox>

                <GroupBox Header="Address Details" Margin="0,0,0,10">
                    <StackPanel Margin="5">
                        <Label Content="Street Address"/>
                        <TextBox Text="{Binding MCustomer.Address}" Height="28" Margin="0,0,0,5"/>
                        <UniformGrid Columns="2">
                            <StackPanel Margin="0,0,5,0">
                                <Label Content="City"/>
                                <TextBox Text="{Binding MCustomer.City}" Height="28"/>
                            </StackPanel>
                            <StackPanel Margin="5,0,0,0">
                                <Label Content="State"/>
                                <TextBox Text="{Binding MCustomer.State}" Height="28"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Financial &amp; Bank" Margin="0,0,0,10">
                    <UniformGrid Columns="2" Margin="5">
                        <StackPanel Margin="5">
                            <Label Content="Opening Balance"/>
                            <TextBox Text="{Binding MCustomer.OpeningBalance}" Height="28"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <Label Content="Bank Name"/>
                            <TextBox Text="{Binding MCustomer.BankName}" Height="28"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <Label Content="Account Number"/>
                            <TextBox Text="{Binding MCustomer.AccountNumber}" Height="28"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <Label Content="IFSC Code"/>
                            <TextBox Text="{Binding MCustomer.IFSCCode}" Height="28"/>
                        </StackPanel>
                    </UniformGrid>
                </GroupBox>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,5,30">
                    <Button Content="Reset" Command="{Binding CustomerResetCommand}" Width="80" Height="35" Margin="5"/>
                    <Button Content="Delete" Command="{Binding CustomerDeleteCommand}" Width="80" Height="35" Margin="5" Background="#FFFFEBEB" Foreground="DarkRed"/>
                    <Button Content="Save Customer" Command="{Binding CustomerSaveCommand}" Width="150" Height="35" Margin="5" Background="#FF007ACC" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
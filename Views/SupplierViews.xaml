<UserControl x:Class="MyWPFCRUDApp.Views.SupplierViews"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1000"
             Background="GhostWhite">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Supplier Directory" Margin="5">
            <DataGrid ItemsSource="{Binding Supplier}" 
                      SelectedItem="{Binding SelectedSupplier}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      SelectionMode="Single"
                      AlternatingRowBackground="#F0F0F0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Supplier Name" Binding="{Binding SupplierName}" Width="*"/>
                    <DataGridTextColumn Header="Contact" Binding="{Binding MobileNumber}" Width="110"/>
                    <DataGridTextColumn Header="Balance" Binding="{Binding CurrentBalance, StringFormat={}{0:N2}}" Width="90"/>
                    <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="50"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="5">
            <StackPanel>

                <GroupBox Header="Business Identity" Margin="0,0,0,10">
                    <StackPanel Margin="5">
                        <Label Content="Supplier Company Name*"/>
                        <TextBox Text="{Binding MSupplier.SupplierName, UpdateSourceTrigger=PropertyChanged}" Height="28"/>

                        <UniformGrid Columns="2" Margin="0,10,0,0">
                            <StackPanel Margin="0,0,5,0">
                                <Label Content="Contact Person"/>
                                <TextBox Text="{Binding MSupplier.ContactPerson}" Height="28"/>
                            </StackPanel>
                            <StackPanel Margin="5,0,0,0">
                                <Label Content="GSTIN"/>
                                <TextBox Text="{Binding MSupplier.GSTIN}" Height="28"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Contact &amp; Location" Margin="0,0,0,10">
                    <StackPanel Margin="5">
                        <UniformGrid Columns="2">
                            <StackPanel Margin="0,0,5,0">
                                <Label Content="Mobile Number*"/>
                                <TextBox Text="{Binding MSupplier.MobileNumber}" Height="28"/>
                            </StackPanel>
                            <StackPanel Margin="5,0,0,0">
                                <Label Content="Email"/>
                                <TextBox Text="{Binding MSupplier.Email}" Height="28"/>
                            </StackPanel>
                        </UniformGrid>

                        <Label Content="Full Address" Margin="0,10,0,0"/>
                        <TextBox Text="{Binding MSupplier.Address}" Height="28"/>

                        <UniformGrid Columns="2" Margin="0,10,0,0">
                            <StackPanel Margin="0,0,5,0">
                                <Label Content="City"/>
                                <TextBox Text="{Binding MSupplier.City}" Height="28"/>
                            </StackPanel>
                            <StackPanel Margin="5,0,0,0">
                                <Label Content="State"/>
                                <TextBox Text="{Binding MSupplier.State}" Height="28"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Financials &amp; Status" Margin="0,0,0,10">
                    <UniformGrid Columns="3" Margin="5">
                        <StackPanel Margin="5">
                            <Label Content="Opening Balance"/>
                            <TextBox Text="{Binding MSupplier.OpeningBalance, StringFormat={}{0:F2}}" Height="28"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <Label Content="Current Balance"/>
                            <TextBox Text="{Binding MSupplier.CurrentBalance}" IsReadOnly="True" />
                        </StackPanel>
                        <StackPanel Margin="5" VerticalAlignment="Bottom">
                            <CheckBox Content="Is Active Supplier" IsChecked="{Binding MSupplier.IsActive}" Margin="0,0,0,8"/>
                        </StackPanel>
                    </UniformGrid>
                </GroupBox>

                <GroupBox Header="Bank Details" Margin="0,0,0,10">
                    <UniformGrid Columns="2" Margin="5">
                        <StackPanel Margin="5">
                            <Label Content="Bank Name"/>
                            <TextBox Text="{Binding MSupplier.BankName}" Height="28"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <Label Content="Account Number"/>
                            <TextBox Text="{Binding MSupplier.AccountNumber}" Height="28"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <Label Content="IFSC Code"/>
                            <TextBox Text="{Binding MSupplier.IFSCCode}" Height="28"/>
                        </StackPanel>
                    </UniformGrid>
                </GroupBox>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,5,30">
                    <Button Content="Reset" Command="{Binding SupplierResetCommand}" Width="80" Height="35" Margin="5"/>
                    <Button Content="Delete" Command="{Binding SupplierDeleteCommand}" Width="80" Height="35" Margin="5" Background="#FFFFEBEB" Foreground="DarkRed"/>
                    <Button Content="Save Supplier" Command="{Binding SupplierSaveCommand}" Width="150" Height="35" Margin="5" Background="#FF007ACC" Foreground="White" FontWeight="Bold"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
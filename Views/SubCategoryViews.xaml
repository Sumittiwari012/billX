<UserControl x:Class="MyWPFCRUDApp.Views.SubCategoryViews"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="AliceBlue">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Registered Sub-Categories" Margin="5">
            <DataGrid ItemsSource="{Binding SubCategory}" 
                      SelectedItem="{Binding SelectedSubCategory}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                    <DataGridTextColumn Header="Sub-Category" Binding="{Binding SubCategoryName}" Width="*"/>
                    <DataGridTextColumn Header="Parent Category" Binding="{Binding CategoryName}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="5">
            <StackPanel>
                <GroupBox Header="Sub-Category Details" Margin="0,0,0,10">
                    <UniformGrid Columns="1" Margin="5">
                        <StackPanel Margin="5">
                            <Label Content="Select Category*"/>
                            <ComboBox ItemsSource="{Binding Categories}" 
                                      SelectedValue="{Binding MSubCategory.CategoryId}" 
                                      SelectedValuePath="Id" 
                                      DisplayMemberPath="CategoryName"
                                      Height="30"/>
                        </StackPanel>

                        <StackPanel Margin="5">
                            <Label Content="Sub-Category Name*"/>
                            <TextBox Text="{Binding MSubCategory.SubCategoryName, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="30" VerticalContentAlignment="Center"/>
                        </StackPanel>
                    </UniformGrid>
                </GroupBox>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,5,20">
                    <Button Content="Reset" Command="{Binding SubCategoryResetCommand}" Width="90" Height="30" Margin="5"/>
                    <Button Content="Delete" Command="{Binding SubCategoryDeleteCommand}" Width="90" Height="30" Margin="5" Background="#FFFFEBEB" Foreground="DarkRed"/>
                    <Button Content="Save Sub-Category" Command="{Binding SubCategorySaveCommand}" Width="150" Height="30" Margin="5" Background="#FF007ACC" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>